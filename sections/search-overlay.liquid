{%- comment -%}
  Results are rendered in JS, which has no access to Liquid's money filter — so
  hand it the shop's own format string. Without this the overlay invents its own
  ("1450.00 EGP") and disagrees with every price elsewhere on the site.
{%- endcomment -%}
<div class="search-overlay" data-search-overlay data-money-format="{{ shop.money_format | escape }}">
  <div class="search-overlay__inner">
    <div class="search-overlay__header">
      <h3 class="search-overlay__title">{{ 'search.title' | t }}</h3>
      <button class="search-overlay__close" data-search-close aria-label="{{ 'actions.close' | t }}">
        {% render 'icon-close' %}
      </button>
    </div>

    <div class="search-overlay__form">
      <div class="search-overlay__input-wrapper">
        {% render 'icon-search' %}
        <input
          type="search"
          class="search-overlay__input"
          data-search-input
          placeholder="{{ 'search.placeholder' | t }}"
          autocomplete="off"
          aria-label="{{ 'search.title' | t }}"
        >
      </div>
    </div>

    <div class="search-overlay__loading" data-search-loading style="display:none;">
      <span class="spinner"></span>
    </div>

    <div class="search-overlay__results" data-search-results style="display:none;">
      <div class="search-overlay__results-list" data-search-results-list></div>
    </div>
  </div>
</div>

<style>
  .search-overlay {
    position: fixed;
    inset: 0;
    z-index: 1001;
    background: var(--color-ivory);
    opacity: 0;
    visibility: hidden;
    transition: all var(--transition-base);
    overflow-y: auto;
  }
  .search-overlay.is-active {
    opacity: 1;
    visibility: visible;
  }
  .search-overlay__inner {
    max-width: 600px;
    margin: 0 auto;
    padding: var(--space-xl);
  }
  .search-overlay__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-xl);
  }
  .search-overlay__title {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    color: var(--color-charcoal);
  }
  .search-overlay__close {
    background: none;
    border: none;
    padding: var(--space-sm);
    color: var(--color-charcoal);
    cursor: pointer;
  }
  .search-overlay__input-wrapper {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-md) var(--space-lg);
    border: 1.5px solid var(--color-pearl);
    border-radius: var(--radius-md);
    transition: border-color var(--transition-fast);
  }
  .search-overlay__input-wrapper:focus-within {
    border-color: var(--color-gold);
  }
  .search-overlay__input-wrapper svg {
    color: var(--color-text-secondary);
    flex-shrink: 0;
  }
  .search-overlay__input {
    flex: 1;
    border: none;
    outline: none;
    font-family: var(--font-body);
    font-size: var(--text-base);
    color: var(--color-charcoal);
    background: transparent;
  }
  .search-overlay__input::placeholder {
    color: var(--color-text-secondary);
  }
  .search-overlay__loading {
    text-align: center;
    padding: var(--space-2xl) 0;
  }
  .search-overlay__results {
    margin-top: var(--space-xl);
  }
  .search-result {
    display: flex;
    gap: var(--space-md);
    padding: var(--space-md) 0;
    border-bottom: 1px solid var(--color-pearl);
    align-items: center;
    color: var(--color-charcoal);
    transition: background var(--transition-fast);
  }
  .search-result:hover {
    background: var(--color-gold-10);
  }
  .search-result__image {
    position: relative;
    width: 60px;
    height: 75px;
    flex-shrink: 0;
    overflow: hidden;
    border-radius: var(--radius-sm);
    background: var(--color-surface);
  }
  .search-result__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  /* Sold-out results stay clickable but read as unavailable. */
  .search-result--sold-out .search-result__image img {
    filter: grayscale(100%);
    opacity: 0.62;
  }
  .search-result__sold-out {
    position: absolute;
    inset: auto 0 0 0;
    background: rgba(46, 42, 51, 0.82);
    color: #FFFFFF;
    font-family: var(--font-sans, 'Montserrat', sans-serif);
    font-size: 7px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    text-align: center;
    padding: 3px 2px;
    line-height: 1;
  }
  .search-result--sold-out .search-result__price {
    text-decoration: line-through;
    opacity: 0.7;
  }
  .search-result__title {
    font-family: var(--font-display);
    font-size: var(--text-base);
    font-weight: var(--font-medium);
    color: var(--color-charcoal);
    margin-bottom: var(--space-xs);
  }
  .search-result__price {
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    color: var(--color-gold);
  }
  .search-result__view-all {
    display: block;
    margin-top: var(--space-xl);
    text-align: center;
  }
  .search-result__none {
    text-align: center;
    padding: var(--space-2xl) 0;
    color: var(--color-text-secondary);
  }
</style>

{% schema %}
{
  "name": "Search Overlay",
  "settings": []
}
{% endschema %}
