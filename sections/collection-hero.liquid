{%- assign collection_image = collection.image -%}
{%- if collection_image == blank -%}
  {%- if collection.handle == 'unisex' -%}
    {%- assign fallback_image = 'https://lh3.googleusercontent.com/aida/ADBb0uihj-VaoUIDnCMzKxoyRaBxwfCB2yxuNh4Ca8__m5Zq4tTiFVn1sZCzjDuoWjFnf1dHTZYfvU0r7DuDPf9aLVo-rdOwWFMi_mu7jyNTVsWJw7asZnNoR5aUB72w2L0sFBKSlSpm5lLzCf6lk17C5z_wqKDmGiP2EcamQhcyae-dVzS8hLbqHeidunyKiraQKBhZ3wAP7haq7ABWt1DjSUKgkVAJ-jpZIbVqEwz5WFUMLvxDy-zHFF4AvA2WGmAvViGLlr8fn0lavg' -%}
  {%- elsif collection.handle == 'men' -%}
    {%- assign fallback_image = 'https://lh3.googleusercontent.com/aida/ADBb0uh-rP-F3Tgib7Nb1QmPfF8Eqz24kqzuWS1XJDuw0BKlMqhfC6FEEP2i7nrVLWpx0O0kif2PnfwwPZ5YtlLGCcrmsGfG8x9VU-SFIE9-pUTeJ-ZqX_Thes9uiIgP5vLZNEifPpgyvYIrqpe1TYMZeV0XXrPtjbkSxy3Zh9Xqfz9USFTYSNc4TRo-CZsnxvGkE_FXAtb_QEnb2ZDVxMPz6eWzDLRV6v_WxVItabPgASGt8bj0TOwK7WZqNA6bhk0l6nzplSwUvmluSg' -%}
  {%- elsif collection.handle == 'women' -%}
    {%- assign fallback_image = 'https://lh3.googleusercontent.com/aida/ADBb0uigq1OmCuY0vlhwXw4g2Hq5Pdzg6iNjk0up0J1BfixlVSKzshkbWn4iSo1KHYRrQjgYPuMTl5fDLGgAmv2tEyxBJRRBLSU4uiI2Y3zeGHQzw_ePt_hwqlQqZC-K0c0q8QtkkPj6bvnP2jty-DqNAhegiFxeXHE4rkZttTjHQoGWhk74pt4o07Qt02N2bAtXFws2ps8YAdmNJSIzc0EKuhgfxF2m8WVg7PhYt2AQ5odcQNTcO5BxUQjy51NdOg648jWXGdOsAlCsmg' -%}
  {%- endif -%}
{%- endif -%}

{%- assign show_overlay_and_text = true -%}
{%- if collection.handle == 'unisex' or collection.handle == 'men' or collection.handle == 'women' -%}
  {%- assign show_overlay_and_text = false -%}
{%- endif -%}

<section class="collection-hero section--flush" data-section-id="{{ section.id }}">
  <div class="collection-hero__wrapper">
    {%- if collection_image != blank -%}
      <img
        src="{{ collection_image | image_url: width: 1440 }}"
        alt="{{ collection.title | escape }}"
        class="collection-hero__image"
        width="1440"
        height="640"
        loading="eager"
      >
    {%- elsif fallback_image != blank -%}
      <img
        src="{{ fallback_image }}"
        alt="{{ collection.title | escape }}"
        class="collection-hero__image"
        width="1440"
        height="640"
        loading="eager"
      >
    {%- else -%}
      <div class="collection-hero__image collection-hero__placeholder"></div>
    {%- endif -%}
    
    {%- if show_overlay_and_text -%}
      <div class="collection-hero__overlay"></div>
      <div class="collection-hero__content">
        <div class="container">
          {% render 'breadcrumb' %}
          <h1 class="collection-hero__title">{{ collection.title }}</h1>
          {%- if collection.description != blank -%}
            <p class="collection-hero__description">{{ collection.description | strip_html | truncate: 160 }}</p>
          {%- endif -%}
          <p class="collection-hero__count">{{ collection.products_count }} {{ 'collection.products_count' | t: count: collection.products_count }}</p>
        </div>
      </div>
    {%- endif -%}
  </div>
</section>

<style>
  .collection-hero__wrapper {
    position: relative;
    min-height: 240px;
    display: flex;
    align-items: flex-end;
    overflow: hidden;
  }
  .collection-hero__image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .collection-hero__placeholder {
    background: linear-gradient(135deg, var(--color-mauve) 0%, var(--color-charcoal) 100%);
  }
  .collection-hero__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, var(--color-charcoal-80) 0%, rgba(46, 42, 51, 0.3) 100%);
  }
  .collection-hero__content {
    position: relative;
    z-index: 2;
    width: 100%;
    padding: var(--space-2xl) 0;
  }
  .collection-hero__title {
    color: var(--color-ivory);
    font-size: var(--text-2xl);
    margin-bottom: var(--space-sm);
  }
  .collection-hero__description {
    color: var(--color-pearl);
    font-size: var(--text-sm);
    max-width: 600px;
    margin-bottom: var(--space-sm);
  }
  .collection-hero__count {
    color: var(--color-mauve);
    font-size: var(--text-xs);
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }
  @media (min-width: 768px) {
    .collection-hero__wrapper { min-height: 320px; }
    .collection-hero__title { font-size: var(--text-3xl); }
  }
</style>

{% schema %}
{
  "name": "Collection Hero",
  "settings": [],
  "presets": [
    {
      "name": "Collection Hero"
    }
  ]
}
{% endschema %}
